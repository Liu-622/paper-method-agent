# 验证计划示例（两个场景的实际输出）

> 演示数据，非真实论文分析。下面的计划由 `node scripts/plan-demo.mjs` 直接生成，
> 内容与界面里点「示例场景」按钮得到的完全一致（同一套确定性规则，不调用模型）。

论文：P1、P2、P3（演示数据）；数据集取值：ETTm1、Weather、Traffic

---

# 示例 A：仅 CPU、1 小时以内 —— 先检查数据处理与评估流程

目标：先把数据划分、采样间隔、指标口径对齐，不训练模型。

本次勾选的待确认风险：训练 / 测试划分（规则 R-02，存在差异）；预测跨度与时长（规则 R-03，存在差异）

共生成 3 个实验，优先级 1 是：先把数据处理与评估流程跑通并对齐口径（ETTm1）

---

# 最小验证实验计划（P1、P2、P3）

- 生成时间：2026/9/17 17:50:05
- 目标数据集：ETTm1
- 设备条件：仅 CPU
- 可投入时间：1 小时以内
- 目标：先跑通流程
- 用到的检查规则：R-02、R-03

> 标「论文给出的设置」的项来自原文并可回溯页码；标「工具建议的设置」的是本工具的建议；
> 标「待确认」的是论文没有写清楚、必须先回原文或向作者确认的参数，本工具不会替你编造。

## 优先级 1｜先把数据处理与评估流程跑通并对齐口径（ETTm1）

**状态**：待确认

**要确认的问题**：在「ETTm1」上，数据划分 / 采样间隔 / 指标计算的实现是否与论文描述一致？

**为什么优先**：优先做这一项，是因为它对应检查项「训练 / 测试划分」（规则 R-02，判定：存在差异）。这一步不依赖训练，能先把"数字是怎么算出来的"对齐，后面的实验才有意义。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 训练/验证/测试划分 | P1：训练 / 验证 / 测试 = 7 : 1 : 2（沿时间轴切分）；P2：训练 / 验证 / 测试 = 7 : 1 : 2；P3：训练 / 验证 / 测试 = 6 : 2 : 2 | 论文给出的设置 |
| 测试区间 | P1：测试集为各序列最后 20%（ETTm1 与 Weather 均覆盖 2018-01-01 至 2018-06-24） | 待确认（论文没写清楚） |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |
| 预处理 | P1：z-score 标准化，统计量仅取自训练集；P2：min-max 归一化到 [0, 1]，统计量取自切分之前的整个数据集；P3：正文只说明「输入经过缩放」，具体方式指向附录 A（需回原文确认） | 待确认（论文没写清楚） |

### 待确认项（论文没写清楚，不要编造）

- 测试区间：论文没写清楚，待确认（P2、P3 没有读到这个字段，必须先在原文里确认，不能沿用默认值）
- 预处理：论文没写清楚，待确认（P3 的取值需要回原文/向作者确认后才能当依据）

### 需要准备

**数据**
- 目标数据集：ETTm1
- 建议只取该数据集的一个子集或一段连续时间窗口，先把流程跑通

**代码**
- 论文/官方仓库的数据处理与评估脚本（若没有，需要先自己实现划分与指标计算）

**环境**
- Python + 论文代码依赖（按论文或仓库说明安装）
- 本步骤不需要 GPU：只做数据处理与指标计算

### 最小操作步骤

- [x] 按论文口径实现/核对数据划分：P1：训练 / 验证 / 测试 = 7 : 1 : 2（沿时间轴切分）；P2：训练 / 验证 / 测试 = 7 : 1 : 2；P3：训练 / 验证 / 测试 = 6 : 2 : 2
- [x] 核对采样间隔：P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级）
- [x] 确认「ETTm1」上的划分方式、采样间隔与评价指标口径与论文一致
- [x] 用同一份评估代码复算指标（P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE）
- [x] 打印并保存每一步的数据形状（训练/验证/测试长度）、时间范围与采样间隔，作为记录
- [x] 用一个小样本跑通「读数据 → 前向 → 计算指标」的最短链路，确认没有实现错误

### 保持一致的条件
- 数据集与时间窗口（两篇论文比较时必须用同一段）
- 划分比例与测试区间（如果论文写了）
- 评价指标定义与计算方式

### 需要改变的变量
- 本步骤不改变模型或训练参数；只改变「是否与论文一致」这一判断

### 应记录的指标
- 数据划分后的各集合长度与时间范围
- 采样间隔（按论文口径换算得到的实际间隔）
- 复算指标与论文报告值的量级/趋势关系（不要求追平数字）

### 什么结果支持当前假设
- 划分比例、测试区间、采样间隔与论文描述一致，且指标能在你自己的数据上复算出合理的量级

### 什么结果不支持当前假设
- 划分或时间范围与论文不一致（例如比例相同但测试区间不同）
- 采样间隔与论文不符，导致跨度对应的实际预测时长不同
- 指标口径不同（例如归一化方式或平均方式不同），数字不可直接对照

### 什么情况下停止 / 改用更小的实验
- 数据无法获取或不完整 → 停止，改为在论文给出的统计量上做口径核对，不训练
- 发现划分/区间不一致且无法还原 → 停止，先向作者确认，不要用替代划分继续比较
- CPU 上跑不动完整数据 → 只保留一个数据集的一小段窗口做流程核对

### 规模说明

资源条件（仅 CPU + 1 小时以内）：建议只做数据与评估流程核对、指标复算，不训练模型；具体耗时取决于论文代码与数据规模，本工具不对时间或显存做任何承诺。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---

## 优先级 2｜用最小规模跑一次基线，确认训练与评估链路可用

**状态**：未开始

**要确认的问题**：在最小规模设置下，模型能否跑出与论文同方向（趋势一致）的结果？

**为什么优先**：对应检查项「预测跨度与时长」（规则 R-03，判定：存在差异）。在流程对齐之后做这一项，才能判断差异来自条件不同还是来自模型本身。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 预测跨度 | P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步 | 论文给出的设置 |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |

### 待确认项（论文没写清楚，不要编造）

- 学习率 / 批大小 / 训练轮数 / 随机种子：论文多数没有写明，属于待确认；先用官方仓库默认值并记录来源

### 需要准备

**数据**
- 同一个数据集（ETTm1），使用与上一步一致的划分结果

**代码**
- 论文/官方仓库的模型代码；建议先用官方默认超参数跑一次

**环境**
- 仅 CPU：建议把模型规模/epoch 压到最小，或直接用官方给出的权重做评估
- 训练细节多数论文没写全 → 先用官方仓库默认值，并把用到的值记录下来

### 最小操作步骤

- [ ] 选一个数据集 + 一个预测跨度：P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步
- [ ] 用论文/仓库默认超参数跑一次最小训练（先不管能不能追平论文数字）
- [ ] 用与上一步完全相同的评估代码计算指标
- [ ] 把本次用到的所有超参数（含默认值来源）逐条记录下来
- [ ] 保存指标结果与运行配置，作为后续比较的基线

### 保持一致的条件
- 数据划分与测试区间（与上一步一致）
- 评估代码与指标口径（与上一步一致）
- 模型结构（不要同时改动结构）

### 需要改变的变量
- 只改变一个训练相关的变量（例如 epoch 或学习率），其余保持不变

### 应记录的指标
- MSE / MAE（或论文使用的指标）
- 与论文报告值的**方向与量级**关系（不是追平）
- 运行配置（超参数、数据窗口）

### 什么结果支持当前假设
- 结果与论文同方向（例如同样优于所复现的基线），且指标在同一量级区间内
- 条件对齐后差异明显缩小，说明原先的"差异"主要来自口径而非模型

### 什么结果不支持当前假设
- 结果与论文方向相反，且在确认实现无误后仍然相反
- 条件对齐前后差异都很大，说明差异不是口径造成的

### 什么情况下停止 / 改用更小的实验
- 训练耗时明显超出你的时间预算 → 立刻改用更小设置：更少 epoch、单变量序列、或只做评估
- 本轮只是最小规模验证 → 不要把它写成"完整复现"
- 1 小时内跑不完一次训练 → 跳过训练，只做评估链路核对

### 规模说明

资源条件（仅 CPU + 1 小时以内）：建议只做数据与评估流程核对、指标复算，不训练模型；具体耗时取决于论文代码与数据规模，本工具不对时间或显存做任何承诺。 注意：最小规模跑通**不等于**复现了论文结果。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---

## 优先级 3｜换一个跨度/数据集，检查结论是否稳健

**状态**：未开始

**要确认的问题**：把跨度或数据集换一个，结论方向是否保持？哪些设置会让结论翻转？

**为什么优先**：作为最后一项，用来检查结论对设置的敏感性，避免只看一个点就下结论。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 预测跨度 | P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步 | 论文给出的设置 |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |

### 需要准备

**数据**
- 复用前面已经准备好的数据与划分

**代码**
- 复用前面的训练与评估代码（本步骤不再改代码）

**环境**
- 与前面相同

### 最小操作步骤

- [ ] 换一个预测跨度（或换一个数据集）重复上一次评估，其余保持不变
- [ ] 把两次结果放在同一张表里，标注每次的实际条件（数据集/跨度/间隔/指标）
- [ ] 检查结论方向是否一致；如果不一致，记录是哪个条件导致的翻转
- [ ] 写一段结论：哪些差异可以用口径解释，哪些不能

### 保持一致的条件
- 评估代码与指标
- 数据划分
- 训练超参数（除被有意改变的变量）

### 需要改变的变量
- 预测跨度（或数据集）

### 应记录的指标
- 同一指标在不同跨度/数据集上的变化
- 结论方向是否一致

### 什么结果支持当前假设
- 结论方向在不同跨度/数据集下保持一致

### 什么结果不支持当前假设
- 只在一个跨度下成立，换跨度后翻转或消失

### 什么情况下停止 / 改用更小的实验
- 如果时间预算用完 → 停止，把已经得到的部分写清楚，不留"待补"的结论
- 如果两次运行的差异完全由数据长度造成 → 只记录口径差异，不下方法优劣结论

### 规模说明

资源条件（仅 CPU + 1 小时以内）：建议只做数据与评估流程核对、指标复算，不训练模型；具体耗时取决于论文代码与数据规模，本工具不对时间或显存做任何承诺。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---


> 上面这份是**勾完优先级 1 的全部步骤、但没有填实际结果**时的导出效果：状态停在「待确认」，
> 文档里明确写着「步骤勾完 ≠ 假设成立」，不会出现"验证成功"这类结论。

---

# 示例 B：单张 GPU、1 天 —— 优先核对一项影响公平比较的实验条件

目标：先确认两篇论文在同一数据集上到底哪些条件不同，再决定要不要跑训练。

本次勾选的待确认风险：训练 / 测试划分（规则 R-02，存在差异）；预测跨度与时长（规则 R-03，存在差异）

共生成 3 个实验，优先级 1 是：先核对影响公平比较的实验条件（ETTm1）

---

# 最小验证实验计划（P1、P2、P3）

- 生成时间：2026/9/17 17:50:05
- 目标数据集：ETTm1
- 设备条件：单张 GPU
- 可投入时间：1 天
- 目标：检查比较是否公平
- 用到的检查规则：R-02、R-03

> 标「论文给出的设置」的项来自原文并可回溯页码；标「工具建议的设置」的是本工具的建议；
> 标「待确认」的是论文没有写清楚、必须先回原文或向作者确认的参数，本工具不会替你编造。

## 优先级 1｜先核对影响公平比较的实验条件（ETTm1）

**状态**：待确认

**要确认的问题**：两篇论文在「ETTm1」上的划分、测试区间、采样间隔与指标口径是否真的可比？差异出现在哪一步？

**为什么优先**：优先做这一项，是因为它对应检查项「训练 / 测试划分」（规则 R-02，判定：存在差异）。这一步不依赖训练，能先把"数字是怎么算出来的"对齐，后面的实验才有意义。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 训练/验证/测试划分 | P1：训练 / 验证 / 测试 = 7 : 1 : 2（沿时间轴切分）；P2：训练 / 验证 / 测试 = 7 : 1 : 2；P3：训练 / 验证 / 测试 = 6 : 2 : 2 | 论文给出的设置 |
| 测试区间 | P1：测试集为各序列最后 20%（ETTm1 与 Weather 均覆盖 2018-01-01 至 2018-06-24） | 待确认（论文没写清楚） |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |
| 预处理 | P1：z-score 标准化，统计量仅取自训练集；P2：min-max 归一化到 [0, 1]，统计量取自切分之前的整个数据集；P3：正文只说明「输入经过缩放」，具体方式指向附录 A（需回原文确认） | 待确认（论文没写清楚） |

### 待确认项（论文没写清楚，不要编造）

- 测试区间：论文没写清楚，待确认（P2、P3 没有读到这个字段，必须先在原文里确认，不能沿用默认值）
- 预处理：论文没写清楚，待确认（P3 的取值需要回原文/向作者确认后才能当依据）

### 需要准备

**数据**
- 目标数据集：ETTm1
- 建议只取该数据集的一个子集或一段连续时间窗口，先把流程跑通

**代码**
- 论文/官方仓库的数据处理与评估脚本（若没有，需要先自己实现划分与指标计算）

**环境**
- Python + 论文代码依赖（按论文或仓库说明安装）
- 本步骤仍然不需要 GPU

### 最小操作步骤

- [x] 按论文口径实现/核对数据划分：P1：训练 / 验证 / 测试 = 7 : 1 : 2（沿时间轴切分）；P2：训练 / 验证 / 测试 = 7 : 1 : 2；P3：训练 / 验证 / 测试 = 6 : 2 : 2
- [x] 核对采样间隔：P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级）
- [x] 确认「ETTm1」上的划分方式、采样间隔与评价指标口径与论文一致
- [x] 用同一份评估代码复算指标（P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE）
- [x] 打印并保存每一步的数据形状（训练/验证/测试长度）、时间范围与采样间隔，作为记录
- [x] 用一个小样本跑通「读数据 → 前向 → 计算指标」的最短链路，确认没有实现错误

### 保持一致的条件
- 数据集与时间窗口（两篇论文比较时必须用同一段）
- 划分比例与测试区间（如果论文写了）
- 评价指标定义与计算方式

### 需要改变的变量
- 本步骤不改变模型或训练参数；只改变「是否与论文一致」这一判断

### 应记录的指标
- 数据划分后的各集合长度与时间范围
- 采样间隔（按论文口径换算得到的实际间隔）
- 复算指标与论文报告值的量级/趋势关系（不要求追平数字）

### 什么结果支持当前假设
- 划分比例、测试区间、采样间隔与论文描述一致，且指标能在你自己的数据上复算出合理的量级

### 什么结果不支持当前假设
- 划分或时间范围与论文不一致（例如比例相同但测试区间不同）
- 采样间隔与论文不符，导致跨度对应的实际预测时长不同
- 指标口径不同（例如归一化方式或平均方式不同），数字不可直接对照

### 什么情况下停止 / 改用更小的实验
- 数据无法获取或不完整 → 停止，改为在论文给出的统计量上做口径核对，不训练
- 发现划分/区间不一致且无法还原 → 停止，先向作者确认，不要用替代划分继续比较
- 流程核对耗时过长 → 只保留最小链路

### 规模说明

资源条件（单张 GPU）：通常可以跑一次单点训练或一次对照，但**能否跑完取决于论文代码、数据规模与显卡型号**，本工具不做显存或耗时承诺。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---

## 优先级 2｜对齐条件后重跑一次对照，看结论是否随口径改变

**状态**：未开始

**要确认的问题**：把两边条件对齐到同一数据集、同一跨度、同一指标后，原先的差异还剩多少？

**为什么优先**：对应检查项「预测跨度与时长」（规则 R-03，判定：存在差异）。在流程对齐之后做这一项，才能判断差异来自条件不同还是来自模型本身。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 预测跨度 | P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步 | 论文给出的设置 |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |

### 待确认项（论文没写清楚，不要编造）

- 学习率 / 批大小 / 训练轮数 / 随机种子：论文多数没有写明，属于待确认；先用官方仓库默认值并记录来源

### 需要准备

**数据**
- 同一个数据集（ETTm1），使用与上一步一致的划分结果

**代码**
- 论文/官方仓库的模型代码；建议先用官方默认超参数跑一次

**环境**
- 单张 GPU（或你自定义的设备）
- 训练细节多数论文没写全 → 先用官方仓库默认值，并把用到的值记录下来

### 最小操作步骤

- [ ] 选一个数据集 + 一个预测跨度：P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步
- [ ] 用论文/仓库默认超参数跑一次最小训练（先不管能不能追平论文数字）
- [ ] 用与上一步完全相同的评估代码计算指标
- [ ] 把本次用到的所有超参数（含默认值来源）逐条记录下来
- [ ] 保存指标结果与运行配置，作为后续比较的基线

### 保持一致的条件
- 数据划分与测试区间（与上一步一致）
- 评估代码与指标口径（与上一步一致）
- 模型结构（不要同时改动结构）

### 需要改变的变量
- 两边条件对齐前后的差异（同一评估管道下比较）

### 应记录的指标
- MSE / MAE（或论文使用的指标）
- 与论文报告值的**方向与量级**关系（不是追平）
- 运行配置（超参数、数据窗口）

### 什么结果支持当前假设
- 结果与论文同方向（例如同样优于所复现的基线），且指标在同一量级区间内
- 条件对齐后差异明显缩小，说明原先的"差异"主要来自口径而非模型

### 什么结果不支持当前假设
- 结果与论文方向相反，且在确认实现无误后仍然相反
- 条件对齐前后差异都很大，说明差异不是口径造成的

### 什么情况下停止 / 改用更小的实验
- 训练耗时明显超出你的时间预算 → 立刻改用更小设置：更少 epoch、单变量序列、或只做评估
- 本轮只是最小规模验证 → 不要把它写成"完整复现"
- 与时间预算冲突时优先缩小模型规模

### 规模说明

资源条件（单张 GPU）：通常可以跑一次单点训练或一次对照，但**能否跑完取决于论文代码、数据规模与显卡型号**，本工具不做显存或耗时承诺。 注意：最小规模跑通**不等于**复现了论文结果。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---

## 优先级 3｜记录口径差异造成的量级差异，判断数字到底能不能直接比

**状态**：未开始

**要确认的问题**：两篇论文的数字放在一起时，误差差异是否大到足以说明"可比性有问题"（而不是单纯的方法优劣）？

**为什么优先**：作为最后一项，用来检查结论对设置的敏感性，避免只看一个点就下结论。

### 关键参数（含来源）

| 参数 | 取值 | 来源 |
| --- | --- | --- |
| 目标数据集 | ETTm1 | 工具建议的设置 |
| 预测跨度 | P1：96 步；P2：96 步；P3：主设置 192 步；同一模型额外迁移到 96 与 336 步 | 论文给出的设置 |
| 采样间隔 | P1：ETTm1 为 15 分钟；P2：ETTm1 与 Weather 均下采样到 15 分钟；P3：1 小时（所有序列重采样到小时级） | 论文给出的设置 |
| 评价指标 | P1：MSE、MAE；P2：MSE、MAE；P3：MSE、MAE、SMAPE、MAPE | 论文给出的设置 |

### 需要准备

**数据**
- 复用前面已经准备好的数据与划分

**代码**
- 复用前面的训练与评估代码（本步骤不再改代码）

**环境**
- 与前面相同

### 最小操作步骤

- [ ] 换一个预测跨度（或换一个数据集）重复上一次评估，其余保持不变
- [ ] 把两次结果放在同一张表里，标注每次的实际条件（数据集/跨度/间隔/指标）
- [ ] 检查结论方向是否一致；如果不一致，记录是哪个条件导致的翻转
- [ ] 写一段结论：哪些差异可以用口径解释，哪些不能

### 保持一致的条件
- 评估代码与指标
- 数据划分
- 训练超参数（除被有意改变的变量）

### 需要改变的变量
- 预测跨度（或数据集）

### 应记录的指标
- 同一指标在不同跨度/数据集上的变化
- 结论方向是否一致

### 什么结果支持当前假设
- 结论方向在不同跨度/数据集下保持一致

### 什么结果不支持当前假设
- 只在一个跨度下成立，换跨度后翻转或消失

### 什么情况下停止 / 改用更小的实验
- 如果时间预算用完 → 停止，把已经得到的部分写清楚，不留"待补"的结论
- 如果两次运行的差异完全由数据长度造成 → 只记录口径差异，不下方法优劣结论

### 规模说明

资源条件（单张 GPU）：通常可以跑一次单点训练或一次对照，但**能否跑完取决于论文代码、数据规模与显卡型号**，本工具不做显存或耗时承诺。

### 我的实际结果

（未填写）

### 我的观察

（未填写）

> 步骤勾完 ≠ 假设成立。你还没有填写「实际结果」，因此这里不会给出任何"验证成功/失败"的结论。

---


> 上面这份是**勾完优先级 1 的全部步骤、但没有填实际结果**时的导出效果：状态停在「待确认」，
> 文档里明确写着「步骤勾完 ≠ 假设成立」，不会出现"验证成功"这类结论。

---
