# 构建时后端地址（把后端烧进前端产物，评委不用填任何配置）
# ------------------------------------------------------------------
# 用法：把下面两行复制成 .env.production 并填上你的后端地址，然后 npm run build
#
#   VITE_API_BASE="https://你的后端地址"
#   VITE_API_TOKEN="可选：与后端 API_ACCESS_TOKEN 相同的口令，留空表示不启用"
#
# 不配置也没关系：页面左下角「运行设置」里可以手动填后端地址（适合本地调试）。
# 配置了以后，静态站点默认就连它，评委打开链接即可使用。

# 示例（部署到 CloudBase 云托管 / Render 后的地址）
# VITE_API_BASE="https://xxxx.service.tcloudbase.com"
# VITE_API_TOKEN=""

# 本地单进程运行（node server/index.mjs，前后端同源）时不需要设置，留空即可
VITE_API_BASE=""
VITE_API_TOKEN=""
