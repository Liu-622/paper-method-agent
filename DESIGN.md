# DESIGN.md · 论文复现助手（Apple 风格适配版）

## 当前交互方向（2026-09-20，优先于下文旧工作台规范）

用户要求：首页极简，只有少量大块入口；点击后浮起展开，再呈现完整内容。
首页采用四块研究空间：论文书房、发现差异、小咕实验室、研究笔记。没有侧栏、统计仪表盘、待办墙或长段介绍。
大块以克制的灰蓝、纸色与深色层次搭配代码绘制的装饰图形；装饰不是实验结果。
业务页面在同一个浮动工作区打开，背景淡化，支持关闭、Esc、放大与深链接，已有内容与数据不减少。
工作区内保留紧凑图标导航、表格和证据操作。阴影只承担浮起和窗口层次，不为每条数据画卡片。
`gallery.css` 仅负责入口和浮动窗口；现有业务组件规则仍由 global/theme/pages 管理。动画尊重减少动态效果设置。
本节明确替代下文“首页不用大磁贴/工作区完全不用投影”的旧限制；不要再次把首页改回密集仪表盘。

本文件是本产品的设计规范。视觉参考：`docs/reference/apple-DESIGN.md`（第三方对 Apple 官网的设计分析，原始链接
https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/apple/DESIGN.md ）。
**注意**：本产品是「论文研究工作台」，不是产品营销页 —— 只借用 Apple 的排版纪律、颜色克制与组件秩序，
**不照搬**整屏海报、超大标题、全出血磁贴与大量留白。

---

## 1. 设计原则（从参考文档适配而来）

| 参考原则 | 本产品的落地方式 |
| --- | --- |
| 单一强调色 `#0066cc`，链接与主操作同色 | 蓝色只用于主操作、链接、焦点环；其余状态用中性灰阶 |
| 唯一真实阴影只给产品摄影 | 工作台**不用投影**：层级靠细边线（#e0e0e0）与背景色变化（白 ↔ 羊皮纸白） |
| 标题 600 字重 + 负字距 | 桌面标题 28–32px / 600；中文**不加负字距**（避免机械套用拉丁字距） |
| 正文 17px/1.47 | 本产品正文 **15–16px / 1.6**（工具型界面信息密度更高，行高便于长文阅读） |
| 胶囊按钮、`scale(0.95)` 按下 | 主操作胶囊；所有按钮按下 `scale(0.96)` |
| 背景模糊的副导航与吸底栏 | 工具条/底部悬浮栏用 `backdrop-filter` 半透明；**正文阅读区保持实底白** |
| 全出血明暗磁贴交替 | 不采用；改用「分组 + 细线分隔」组织长内容 |

## 2. 设计变量（唯一来源：`src/styles/global.css` 的 `:root`）

```
背景 canvas        #ffffff
羊皮纸 parchment   #f5f5f7   （页面背景、次级表面、工具栏底色）
正文 ink           #1d1d1f
次级文字           #7a7a7a
更弱文字           #a1a1a6
边线 hairline      #e0e0e0
软分隔             rgba(0,0,0,0.06)
主操作 primary     #0066cc
焦点环             #0071e3
```

业务需要的状态色**降到最低限度**，且始终配文字标签：
`--warn #9a6700`（需要关注）、`--danger #b42318`（失败）、`--ok #1f7a4d`（已确认）。
论文识别色（用于区分三篇，不表达状态）：`--p1 #0066cc`、`--p2 #9a6700`、`--p3 #6a5f8a`。

## 3. 排版

- 字体栈（跨平台，中文优先系统字体）：
  `-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI Variable", "Segoe UI",
   "PingFang SC", "Microsoft YaHei UI", "Microsoft YaHei", system-ui, sans-serif`
- 页标题 30px/600；区块标题 15px/600；正文 15px/400/1.6；说明 13px；细则 12px。
- 数字用 `font-variant-numeric: tabular-nums`，表格/取值列对齐。
- 关键词加粗不超过每句 2–3 处（`InsightBits.Highlighted`，结构化节点，不注入 HTML）。
- 数据、比例、单位必须能被加粗识别（如 `6:2:2`、`96`、`ETTm2`）。

## 4. 布局与组件

- 工作区：侧栏 224px + 内容区，内容最大宽 1180px，居中留白。
- 侧栏：羊皮纸底、细线右边界；导航项选中 = 中性填充 `#f5f5f7` + 近黑文字（**不用彩色底**）。
- 卡片/面板：白底 + 1px `#e0e0e0` + 圆角 14–18px（**无阴影**）。
- 按钮：主操作胶囊蓝；次级为幽灵胶囊（1px 蓝边 + 蓝字）；工具按钮为浅灰胶囊；危险操作红字。
- 输入：白底、1px `rgba(0,0,0,0.08)`、圆角 10px、高 40px；焦点 3px `#0071e3` 环。
- 标签（Tag）：默认中性；只有「状态」标签才带色，且必须有文字。
- 工具条 / 底部悬浮栏 / 抽屉：半透明白 + `saturate(160%) blur(20px)`；正文区不透明。
- 空状态：小咕插画 + 一句定位 + 一个明确主操作。

## 5. 动效

- 统一 150–250ms；`cubic-bezier(0.32, 0.72, 0, 1)`。
- 按钮按下 `scale(0.96)`；卡片悬停上移 2px（无阴影，仅边线变深）；抽屉右滑入 220ms；字段更新高亮 1.4s。
- 禁止：假进度、持续闪动、滚动劫持、入场动画重复播放。
- `prefers-reduced-motion: reduce` 时全部关闭，功能完整。

## 6. 小咕（品牌角色）

- `src/components/Buddy.tsx` 纯 SVG：奶油色身体、鼠尾草绿翅膀、大眼、短耳羽、抱小论文。
- 白灰界面下加深轮廓（近黑描边），40px 仍可辨认；不用 emoji、不在方块里放字、不使用 Apple 标志。
- 三种用法：侧栏品牌、问答区、右下角助手入口（同一组件）。
- 交互：低频自然眨眼；悬停轻微歪头 + 一句短提示；点击打开研究助手；请求中思考、成功短暂反馈、失败困惑 + 重试；
  可收起、不遮挡正文、不主动抢焦点。

## 7. 正确性约束（设计不得违反）

视觉再好看也不能改变下列事实边界：
「未找到 / 未检查 / 不适用 / 需确认」四态必须准确区分；引用存在 ≠ 支持结论；
未知值不得覆盖已知差异；只勾步骤或只填观察不能标记完成；完成记录 ≠ 假设成立；
不为美观隐藏缺口、编造摘要或注入固定答案。

## 8. 小咕实验室（v1.2）

- 新页面 `/lab`：左侧实验条件、中间真实曲线与「结论适用地图」、右侧小咕的实验记录与预算。
- 沿用既有组件与配色（卡片、chip、Tag、小咕），不引入图表库；曲线是自绘 SVG，支持悬停读数与序列切换。
- 地图格子的状态色属于**状态**语义，不是装饰色：未测试（斜纹灰）/ 运行中（蓝）/ 已完成（浅绿）/ 差距较小（琥珀）/ 失败（红）。
- 只有真实执行过的格子才有数字；失败格子写原因；未测试格子保持未测试。
- 动效仍是 150–250ms，且 `prefers-reduced-motion` 下全部关闭。
